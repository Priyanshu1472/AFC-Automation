# Copy to .env for local `docker compose up` — never commit the real values.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Optional — defaults shown.
# PROPOSAL_BUCKET=proposal-documents
# PROPOSAL_WORKER_CONCURRENCY=2
# PROPOSAL_WORKER_POLL_INTERVAL=10
# PROPOSAL_WORKER_LO_TIMEOUT=90
# PROPOSAL_WORKER_MAX_OUTPUT_BYTES=26214400
