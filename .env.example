# Copy this file to .env.local and fill in the values from your Supabase
# project's Settings -> API page. .env.local is gitignored — never commit
# real values.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
